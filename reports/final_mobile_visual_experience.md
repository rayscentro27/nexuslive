# Final Mobile Visual Experience

- Mobile/tablet behavior validated through Playwright desktop + attempted tablet/mobile runs.
- Cards and KPI bars remain wrapped/flexible; compact chips and short labels preserve readability.
- Motion remains restrained on smaller screens (pulse dots/bars rather than heavy transforms).
- Known environment blocker: tablet/mobile Playwright runs require WebKit install (`npx playwright install`).
